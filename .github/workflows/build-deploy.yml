name: Build and Deploy Code

on: [push, pull_request]
  
jobs:
  job1:
    runs-on: ubunto-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v4
      - name: say hello to hash
        run: echo " Hello HasH "